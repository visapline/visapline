// types/theme.ts

export interface ThemeType {
    colors: {
      background: string;
      background2: string;
      text: string;
      primary: string;
      secondary: string;
      hovertext: string;
    };

    fonts: {
      primary: string;
      secondary: string;
    };
  }
  